# UNWORK 학습 실행 Agent — 구현 범위

기준일 2026-08-26 · 범위: 백엔드 · 지금까지 GPU 과금 0원

---

## A. 제출용 표 (부스 시연 기준)

한 사용자가 평범하게 서비스를 한 번 실행하는 흐름에서 그대로 보이는 기능들이다.

| 기능 | 구현 상태 | 시연 | 비고 |
| --- | --- | --- | --- |
| 예산과 완료 우선순위만으로 실행 환경 결정 | 완료 | O | 사용자는 GPU 종류, 서버, 이미지, SSH를 다루지 않는다. 입력은 얼마를 쓸지와 무엇을 우선할지 두 가지뿐이다. |
| GPU 후보 비교와 추천 근거 제시 | 완료 | O | 후보별 예상 실행시간과 예상 비용을 함께 보여주고, 왜 이 GPU를 골랐는지 근거를 제시한다. 우선순위를 바꿔 다시 실행하면 다른 GPU가 선택되는 것도 같은 화면에서 확인된다. |
| 실행 계약 승인 | 완료 | O | 승인 전에는 비용이 발생하지 않는다. 승인하면 화면에 보인 GPU와 조건이 그대로 실행된다. |
| 학습 실행과 진행 상태 확인 | 완료 | O | 실행 환경 준비 중인지, 학습이 돌고 있는지, 정리 중인지를 사용자가 계속 확인할 수 있다. 브라우저를 닫아도 서버가 실행을 이어서 책임진다. |
| 완료 결과 전달 | 완료 | O | 완료 로그, 종료 코드, 실행 시간, Agent가 선택한 GPU를 결과 화면에서 함께 전달한다. |
| 작업 종료 후 GPU 자동 종료 | 완료 | O | 학습이 끝나면 Agent가 GPU 삭제를 요청하고 종료를 확인한다. 종료가 확인되기 전에는 완료로 표시하지 않는다. 실제 Runpod GPU에서 생성부터 종료 확인까지 50초에 완주하는 것을 확인했다. |

### 시연 시 밝힐 전제

부스 데모의 GPU 생성·종료는 실제 Runpod GPU 대신 동일한 상태 전이를 재현하는 데모 실행기로 진행한다. 실제 계정 연동은 다음으로 확인할 수 있다.

- 실제 Runpod API 키 인증 성공, 계정 활성 GPU 0개
- 데모 GPU 3종이 모두 실제로 제공되는 사양임을 확인 (`--check-env`, GPU를 만들지 않아 비용 없음)

### 함께 적을 수 있는 한 줄

실제 GPU에서 학습까지 끝냈으므로 이 줄은 이제 완료로 적을 수 있다.

| 기능 | 구현 상태 | 시연 | 비고 |
| --- | --- | --- | --- |
| 실제 GPU에서 학습 수행 | 완료 | O | 실제 Runpod GPU에서 SD 1.5 LoRA 학습을 수행하고 결과 전달·GPU 종료 확인까지 완주했다. 부스 시연은 관람객을 기다리게 하지 않으려고 시뮬레이터로 진행한다. |

---

## B. 부스 시연 시나리오 (약 2분)

한 번의 정상 실행을 처음부터 끝까지 보여준다.

| 순서 | 보여주는 것 | 소요 |
| --- | --- | --- |
| 1 | 예산 1,000원과 우선순위(빠른 완료) 입력 | 20초 |
| 2 | Agent가 비교한 GPU 후보 3종과 추천 GPU, 선택 근거 확인 | 25초 |
| 3 | 실행 계약 승인 | 10초 |
| 4 | 실행 환경 준비 중 → 학습 실행 중 진행 상태 확인 | 30초 |
| 5 | 완료 화면에서 완료 로그, 종료 코드, 실행 시간, 선택된 GPU, GPU 종료 완료 확인 | 25초 |

핵심 메시지는 2번과 5번이다. **사용자는 GPU를 고른 적이 없는데 Agent가 조건에 맞는 GPU를 골라 실행했고, 끝난 뒤 GPU까지 스스로 정리했다.**

### 질문이 나올 때만 보여줄 예비 시연

먼저 꺼내지 않는다. 관람객이 물어볼 때 하나씩 보여준다.

| 질문 | 보여줄 것 | 소요 |
| --- | --- | --- |
| "우선순위를 바꾸면 정말 달라지나요?" | 같은 예산에 저비용·균형으로 다시 실행 → 다른 GPU 추천 | 30초 |
| "예산이 모자라면요?" | 예산을 낮춰 입력 → 실행안 없음 안내, 작업 생성되지 않음 | 15초 |
| "사용자가 잊어버리면 GPU가 계속 켜져 있나요?" | 상한을 1분으로 낮춘 서버에서 방치된 작업이 스스로 종료되는 과정 (실측 67초) | 70초 |
| "중간에 멈출 수 있나요?" | 실행 중 중단 → GPU 종료 확인 후 중단 완료 | 20초 |

### 시연 준비

```bash
cd ai-training-cost-optimizer
export MVP_PROVIDER_MODE=fake MVP_COOKIE_SECURE=false MVP_MAX_RUNTIME_MINUTES=10
python -m uvicorn training_cost_optimizer.api:app --port 8000
```

- 자동 종료 예비 시연만 `MVP_MAX_RUNTIME_MINUTES=1`로 서버를 다시 띄워 진행한다.
- 화면이 준비되기 전에는 `python -m training_cost_optimizer.mvp.rehearsal`로 전체 흐름이 자동으로 지나가는 것을 보여줄 수 있다.

---

## C. 참고: 제품 기능 전체(PRD) 대비 구현 수준

A의 표는 부스에서 보여줄 수 있는 범위이고, 아래는 제품이 최종적으로 제공하기로 한 기능 전체 기준이다.

| 기능 | 구현 상태 | 시연 | 비고 |
| --- | --- | --- | --- |
| 학습 요구사항 분석 (필요 VRAM·예상 실행시간) | 부분 구현 | O | 모델·학습 방식·데이터 크기 기반 규칙 분석은 동작. 사용자 Repository와 실행 명령 직접 분석은 미구현 |
| GPU 실행안 비교와 추천 | 부분 구현 | O | 작업 완료 비용 기준 비교와 선택 근거 제시. 사전 검증 GPU 3종 범위에서는 완료, 다중 공급자 실시간 가격·위험도·대체 후보는 미구현 |
| 비용 발생 전 실행 계약 승인 | 완료 | O | 승인 전 무과금, 승인 후 조건 고정, 중복·동시 실행 차단 |
| GPU 실행 환경 생성과 학습 실행 | 완료 | O | 실제 Runpod GPU(RTX 4090)에서 SD 1.5 LoRA 학습을 끝까지 수행했다. 학습 50 step, 가중치 저장, 완료 신호 전달, GPU 삭제·종료 확인까지 완주 |
| 실행 상태 감시와 상한 초과 자동 중단 | 부분 구현 | O | 실제 GPU에서 상태 추적을 확인했다. 시간 초과 종료는 아직 로컬에서만 확인. 실시간 누적 비용 계측 기반 중단은 미구현 |
| 모든 종료 경로에서 자원 종료 확인 | 부분 구현 | O | 성공 경로는 실제 GPU에서 삭제·종료 확인까지 검증했다. 실패·중단·시간 초과 경로는 아직 로컬에서만 확인 |
| 결과 artifact·checkpoint 보관과 전달 | 미구현 | X | 완료 로그·종료 코드·실행 시간 전달까지만 구현 |
| 사용자 공급자 계정 연결 (BYOK) | 완료 | O | 사용자가 본인 Runpod 키를 연결하면 그 계정에서 GPU가 만들어진다. 키는 받는 즉시 실제 호출로 유효성을 확인하고, 메모리에만 보관하며 응답·로그에 반환하지 않는다. 팀 키로 대신 실행하지 않는다 |
| 다중 공급자 비교 | 미구현 | X | Runpod 하나만 연동돼 있다. 공급자 간 가격·가용성 비교는 다음 단계다 |

---

## D. 검증 근거

| 명령 | 결과 |
| --- | --- |
| `python -m pytest -q` | 자동화 테스트 120개 통과 (추천 정책, 승인 원자성, 실행 생애주기, 중단·실패·시간 초과, 비밀정보 비노출) |
| `python -m training_cost_optimizer.mvp.rehearsal` | 실제 서버를 띄워 세션 → 추천 → 승인 → 상태 확인 → 완료 → 중단 → 세션 격리까지 22개 항목 전부 통과 |
| `python -m training_cost_optimizer.mvp.smoke --check-env` | 실제 Runpod 키로 설정과 데모 GPU 3종의 유효성 확인. GPU를 만들지 않으므로 비용 없음 |

실제 Runpod GPU(RTX 4090)로 한 번 완주했다. 상태 전이는 `PROVISIONING(3초) → RUNNING(9초) → TERMINATING(45초) → COMPLETED(50초)`였고, GPU 안에서 보낸 완료 신호가 백엔드에 도달했으며 종료 확인 뒤 계정에 남은 GPU는 0개였다. 소요 비용은 10원 미만이다.

## E. 실제 GPU 실행까지 남은 조건

1. ~~**공개 주소**~~ — 해결됐다. 완료 신호가 실제 GPU에서 백엔드까지 도달하는 것을 확인했다.
2. ~~**학습 컨테이너 이미지**~~ — 해결됐다. 이미지를 만들어 레지스트리에 올렸고 실제 GPU에서 학습이 완주했다.
3. **사용자 화면** — 확정된 API 계약에 맞춘 입력·비교·진행·결과 화면. 프런트엔드에서 진행 중이다.

남은 것은 화면뿐이다. artifact 보관, 사용자 계정 연결, 다중 공급자 비교는 그다음 단계다.

### 비용을 누가 내는가

BYOK 이후 GPU 사용료는 사용자가 연결한 본인 계정에서 직접 발생한다. 우리가 GPU 사용료를 청구하는 구조가 아니다. Agent 실행 수수료는 향후 과금 모델이며 현재 청구하지 않는다.

### 실행 시간에 대해 알아둘 것

실제 GPU 실행 한 번의 시간 분포는 준비 7분 30초, 학습 7초였다. Runpod의 이미지 전송 속도(약 13MB/s)가 준비 시간을 지배한다. 그래서 실제 실행에는 상한을 15분으로 두고, 부스 시연은 같은 상태 전이를 즉시 재현하는 시뮬레이터로 진행한다.

## 참고

- 표시되는 예상 시간과 GPU 비용은 데모 전에 검증한 프로필 스냅샷이며 실제 청구액을 보장하지 않는다.
- 현재 백엔드는 단일 서버 프로세스를 전제로 하며, 서버 재시작 후 실행 복구와 다중 서버 분산은 포함하지 않았다.
