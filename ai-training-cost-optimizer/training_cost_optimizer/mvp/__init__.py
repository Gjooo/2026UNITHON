"""Restricted GPU-selection MVP API.

This package intentionally does not reuse the legacy optimizer HTTP models or
job state machine.  It owns the immutable recommendation contract described in
``Backend-implementation-plan.md``.
"""

