# UNWORK 학습 실행 Agent — 최종 PRD

## 한 줄 정의

실행 가능한 AI 학습 코드를 가진 개발자가 **코드·실행 명령·예산·완료 조건을 제공하고 실행 계획을 승인하면**, Agent가 공급자별 학습 환경을 비교·선택하고 실행·감시·결과 전달·VM 종료까지 수행하는 학습 실행 Agent.

## 제품 목표

이 제품은 AI 학습 실행에 수반되는 GPU 인프라 운영 업무를 자동화해, 개발자가 학습 목표와 결과 판단에 집중하도록 돕는다. 사용자는 학습의 목표, 투자할 예산, 완료 기준, 예외 상황에서 계속 투자할지처럼 사람이 내려야 할 판단만 한다. Agent는 GPU 후보 조사, VM 생성, 환경 준비, 실행·비용 감시, artifact 보관, VM 종료를 수행한다.

### MVP 목표

- **인프라 직접 조작 제거**: 지원되는 Training Job에서 사용자가 공급자 콘솔, SSH, CUDA 설치, VM 종료를 직접 하지 않고 결과까지 도달한다.
- **비용 발생 전 통제권 확보**: 실행 전 예상 시간·비용·자동 중단 조건을 제시하고 사용자의 승인을 받는다.
- **Job 생애주기 완결**: VM 생성, 환경 준비, Training 실행, artifact 보관, 자원 정리를 하나의 Job으로 관리한다.
- **실패해도 자원 방치 방지**: 성공·실패·취소·예산 도달 모두에서 VM 종료를 시도한다.

정상 경로에서 사용자는 GPU 공급자 콘솔·SSH·비용 탭을 열거나 종료 버튼을 누르지 않는다.

## 배경

Colab이나 로컬 환경으로는 원하는 AI 학습을 완료하기 어려운 개발자는 외부 GPU가 필요해진다. 이때 공급자별 GPU 사양·가격·가용성을 비교하고, VM을 생성하고, 의존성을 준비하고, 학습 상태와 비용을 감시하고, 결과물을 회수한 뒤 종료해야 한다. 이 일은 모델을 개선하는 일이 아니지만, 개발자의 시간을 빼앗고 학습 지연·예산 초과·미종료 비용을 만든다.

사용자는 GPU 자체를 관리하고 싶은 것이 아니라 학습 결과를 얻고 싶다. 그러나 직접 GPU 클라우드를 이용하면 다음의 인지 노동이 남는다.

- 어떤 공급자와 GPU가 현재 작업에 적합한지 판단
- CUDA·Python·의존성과 데이터 접근 환경 구성
- 실행 중 오류·비용·종료 여부를 계속 확인
- 실패 후 로그·결과·비용을 정리하고 VM 종료

이 MVP가 GPU 공급자 가입, 결제수단 등록, 최초 API 키 발급을 없애는 것은 아니다. 이는 첫 외부 GPU 사용자에게 남는 온보딩 마찰이며, 서비스는 이후 발생하는 실행·감시·종료 업무를 없앤다.

## 문제 및 해결 방안

## 사용자 문제

Colab 또는 로컬에서 VRAM 부족·세션 종료·실행시간 한계를 경험한 AI 개발자는 학습 코드가 있어도 GPU 선택, VM 생성, 환경 구축, 비용 감시, 결과물 회수, 종료를 직접 수행해야 한다. 특히 다음 문제가 반복된다.

- **의사결정 부담**: 필요한 VRAM·GPU·공급자를 정하기 어렵고, 시간당 단가가 실제 작업 완료 비용을 대표하지 않는다.
- **환경 구축 부담**: 의존성, CUDA 호환성, 모델·데이터 접근, 실행 명령 준비가 필요하다.
- **비용 불확실성**: 실행시간, 재시도, 종료 시점 때문에 Job 단위 비용을 예측하기 어렵다.
- **지속적인 관리 부담**: 정상 실행·오류·누적 비용·결과 저장·VM 종료를 계속 확인해야 한다.

## 해결 방안

사용자는 작업에 사용할 GPU VM 공급자의 API 키를 연결하고, 공개 Repository URL, 실행 명령, 최대 예산, 완료 조건을 입력한다. Agent는 지원 여부와 최소 실행 요구사항을 확인한 뒤, 사전 측정한 벤치마크와 공급자별 가격·가용성 정보를 바탕으로 `가장 저렴함`, `가장 빠름`, `균형형` 세 실행안을 제시하고, 예산 내 최저 예상 비용 안을 추천한다.

사용자가 실행 계약을 승인하면 Agent는 선택된 공급자에 VM을 생성하고, 지원되는 Training Job을 실행·감시한다. 완료 후 artifact를 작업별 임시 저장소에 보관해 24시간 만료 링크로 전달하고 VM을 자동 종료한다.

### 사람과 Agent의 역할

| 사람의 판단 | Agent가 없애는 운영 |
| --- | --- |
| 학습 목표와 완료 조건 설정 | GPU 후보 조사·추천 |
| 최대 예산과 최대 실행시간 설정 | VM 생성·환경 준비 |
| 실행 계약 승인 | 학습 실행·로그·비용 감시 |
| 예외 시 계속 투자 여부 결정 | artifact 보관·VM 자동 종료 |

GPU 유형, 예상 비용 또는 예상 시간이 바뀌는 모든 재계획은 새 실행 계약으로 다시 승인받는다.

### 실행 계약

승인 전 사용자는 다음을 확인한다.

- GPU 공급자·후보와 선택 이유
- 예상 시간, GPU 예상 사용료, Agent 실행 수수료(`₩0 · 데모`), 예상 총비용
- 위험도와 대체 후보
- 최대 실행시간과 예산 90% 자동 중단 조건
- 지원 학습 흐름/템플릿 버전

`예상 총비용 = 예상 GPU 사용료 + 저장소/전송비(해당 시) + Agent 실행 수수료`

GPU 사용료는 사용자가 선택한 공급자 계정에 직접 청구된다. 비용 표시는 `경과 시간 × 승인된 시간당 GPU 가격`의 추정치이며 실제 청구액과 차이가 날 수 있음을 고지한다. 중단 위험은 총비용에 숨기지 않고 위험도와 대체 후보로 표시한다.

### Job 생애주기와 예외 정책

```text
Draft → 실행 계획 생성 → 승인됨 → VM 생성 → 환경 준비 → Running
→ artifact 보관 → VM 종료 → Completed
```

실행 화면은 `GPU 후보 선정 완료 → VM 생성 완료 → 환경 준비 완료 → 학습 감시 중 → artifact 보관 완료 → VM 자동 종료 완료` 타임라인을 보여 주며, 각 자동 단계에 `사용자 조작 없음`을 표시한다.

| 상황 | Agent 행동 | 사용자 행동 |
| --- | --- | --- |
| 분석 실패 | 지원 불가 또는 부족한 입력 원인 표시 | 입력 수정 후 새 계획 생성 |
| OOM | 더 큰 VRAM 후보로 1회 재계획 | 새 계약 승인 |
| 인프라/Spot 중단 | checkpoint에서 1회 재개 | 재개 실패 시 계속/중단 결정 |
| 예산 80% | 경고 알림 | 필요 시 중단 결정 |
| 예산 90% | 자동 중단·checkpoint 보관 시도 | 계속하려면 새 계약 승인 |
| 코드/의존성 오류 | 자동 중단·오류 로그 전달 | 수정 후 새 실행 |
| 사용자 취소 | Training 중단 요청, 로그 보관, VM 종료 | 취소 확인 |
| 앱/브라우저 종료 | 독립 워커가 종료 재시도 | VM ID와 직접 종료 방법 제공 |

## 차별점

시간당 GPU 가격을 비교하거나 GPU를 대여하는 서비스가 아니다. 이미 GPU를 사용할 수 있는 팀이 학습 한 건을 실행하기 위해 수행하던 여섯 단계를 실행 계약 하나로 대체한다.

| 기존 반복 학습 실행 | MVP 실행 |
| --- | --- |
| GPU·공급자 비교 | 학습 목표·예산·완료 조건 설정 |
| VM 생성 | 실행 계약 승인 |
| SSH/환경 설정 | artifact 수령 |
| 학습 실행 |  |
| 비용 감시 |  |
| artifact 회수·VM 종료 |  |

따라서 이 제품은 “더 빠르게·쉽게”가 아니라, 사람이 수행하던 인프라 운영을 “더 이상 존재하지 않게” 만든다. 사람은 학습 목적과 비용 투자 판단을, Agent는 정형화된 실행과 감시를 맡는다.

## 타겟 및 시나리오

## 타겟 사용자

주 타겟은 Colab이나 로컬 환경의 한계를 넘어 외부 GPU가 필요해졌지만, 클라우드 GPU 인프라 운영에는 익숙하지 않은 AI 개발자다. 첫 번째 외부 GPU 실행이 필요한 AI 전공 학부생, 대학원 연구자, 주니어 AI 개발자, 개인 프로젝트 개발자를 초기 사용자 세그먼트로 둔다.

이들은 실행 가능한 PyTorch 또는 Hugging Face 학습 코드가 있고, 서버 운영을 배우는 것이 목적은 아니다. 장기적으로는 반복 학습을 하는 소규모 팀에 팀 예산·승인 정책·실행 이력을 제공하는 팀 운영 서비스로 확장한다.

### MVP 지원 범위

- 제품 방향: 다양한 GPU VM 공급자의 가격·가용성을 비교하고 실행할 수 있는 공급자 어댑터 구조
- 실제 실행 검증: Runpod 어댑터 1개
- 제품 지원 방향: 공개 Repository와 실행 명령을 기반으로 한 Training Job
- MVP/데모 실행: 사전 검증한 단일 Golden Path Training Repository 및 실행 명령
- 데이터: 공개 데모 데이터셋
- 자원: 사전에 정의한 6~10개 GPU 후보, GPU 1개·VM 1개
- 결과물: 24시간 만료 다운로드 링크

MVP에서 실제 연동하지 않은 공급자에 대해 실행 가능하다고 주장하지 않는다.

### MVP 비목표

- 모든 GitHub/PyTorch Repository 실행 지원 또는 사용자 코드 자동 수정
- 세계 최저가 GPU 보장, 완벽한 시간·비용 예측
- 사용자 데이터 업로드와 영구 파일 보관
- 멀티 GPU·분산 학습·Notebook IDE·MLOps 전체·모델 배포
- 팀 Workspace·조직 권한·실제 수수료 결제·정산
- 공급자 장애 시 자동 마이그레이션

## 사용자 시나리오

Colab에서 학습을 시도하던 AI 개발자는 VRAM 부족 또는 세션 종료를 겪고 외부 GPU를 사용하기로 한다. 사용자는 GPU 공급자 가입·결제수단 등록·API 키 발급을 마친 뒤, 공개 Repository URL, 실행 명령, 최대 예산, 완료 조건을 입력하고 사용할 공급자의 API 키를 작업에 연결한다. Agent가 공급자·GPU 유형이 포함된 세 가지 실행안을 제안하면 사용자는 하나의 실행 계약을 승인한다.

학습이 진행되는 동안 사용자는 실행 타임라인과 누적 추정 비용을 확인하지만 정상 경로에서는 추가 조작을 하지 않는다. 완료 후 사용자는 24시간 동안 유효한 artifact 다운로드 링크와 총 소요시간·추정 GPU 비용·VM 자동 종료 결과를 받는다.

데모에서는 사전 검증한 Golden Path 하나를 실제 Runpod에서 `VM 생성 → 짧은 학습 → artifact 전달 → VM 종료`까지 완주한다. OOM·예산 도달·인프라 중단은 같은 상태 머신을 통과하는 결정론적 이벤트로 시뮬레이션한다.

## 성공·위험 요소

## 핵심 지표

MVP의 핵심 지표는 **지원 학습 흐름에서 사용자가 서버를 직접 조작하지 않고 artifact 전달과 VM 종료까지 완료한 비율**이다.

보조 지표는 실행 성공률, 예산 한도 내 종료율, VM 자동 종료 성공률, artifact 전달 성공률, 제거된 사용자 운영 단계 수(목표: 6개), 실행 계약 승인율이다.

## 리스크

| 리스크 | 정책 및 완화 |
| --- | --- |
| GPU 가격·가용성 변동 | 실행 계획에 시각·가격 기준을 표시하고, 변경 시 재계획·재승인 |
| 첫 사용자의 온보딩 이탈 | 가입·결제·API 키 발급은 자동화 범위가 아님을 고지하고, 연결 전 안내와 이후 실행 운영 제거의 가치를 분리해 설명 |
| API 키 유출 | 작업 중에만 사용하고 완료·중단·오류 후 즉시 폐기, 로그·분석에 미기록 |
| 자동 종료 실패 | 최대 실행시간 종료 타이머와 독립 워커 재시도, VM ID·직접 종료 방법 제공 |
| 사용자 기존 리소스 오조작 | Agent가 생성한 `unwork-job-<id>` VM만 조회·종료 |
| artifact 유실·노출 | VM 종료 전 임시 저장소로 업로드, 24시간 뒤 자동 삭제 |
| 데모 실패 | 사전 검증한 환경·작은 공개 데이터셋·10~30 step·하드 실행시간 제한 사용 |
