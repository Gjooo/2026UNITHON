# UNWORK 학습 실행 Agent — 최소 MVP PRD

## 한 줄 정의

AI 개발자가 실행 계획을 승인하면, Agent가 검증된 학습 환경을 만들고 실제 GPU 학습을 실행한 뒤 Pod 종료까지 처리하는 학습 실행 Agent.

## 제품 목표

이 제품은 AI 학습 실행에 수반되는 GPU 인프라 운영 업무를 자동화해, 개발자가 학습 목표와 결과 판단에 집중하도록 돕는다.

최소 MVP는 사용자가 GPU 공급자 콘솔, SSH, CUDA 설정, Pod 종료를 직접 다루지 않고 하나의 검증된 학습 작업을 끝내는지를 검증한다.

## 배경

Colab이나 로컬 환경에서 학습하던 개발자는 VRAM 부족, 세션 종료, 실행시간 제한을 만나면 외부 GPU를 써야 한다. 하지만 GPU·VM 선택, 환경 설정, 실행 상태 확인, 서버 종료까지 새 업무가 따라온다.

사용자는 GPU를 운영하는 것이 목적이 아니라 학습을 실행하는 것이 목적이다. MVP는 이 실행 과정 중 사람이 반복하는 인프라 조작을 Agent가 대신 수행할 수 있는지 확인한다.

## 문제 및 해결 방안

## 사용자 문제

외부 GPU가 필요한 AI 개발자는 학습 코드가 있어도 GPU VM을 만들고, 실행 환경을 준비하고, 학습 상태를 확인하고, 작업 뒤 Pod를 종료해야 한다. 이러한 일은 학습 결과와 직접 관계없는 운영 업무지만, 개발자의 시간과 주의를 지속적으로 요구한다.

## 해결 방안

MVP는 사전 검증한 **SD 1.5 LoRA 단일 Golden Path**를 제공한다. 사용자는 고정된 데모 시나리오와 실행 계획을 확인하고 `실행 승인`만 한다.

백엔드는 팀의 Runpod 계정으로 사전 검증된 이미지와 단일 GPU Pod를 생성한다. 학습 컨테이너는 완료 또는 실패 상태를 백엔드에 알리고, 백엔드는 최대 10분 timeout 안에서 Pod 상태를 관리한다. 학습이 끝나거나 실패·취소되면 Pod 삭제를 요청하고 실제 종료를 확인한다.

### 사람과 Agent의 역할

| 사용자 | Agent |
| --- | --- |
| 데모 시나리오와 실행 계획 확인 | Pod 생성 |
| 실행 승인 | 사전 검증된 환경 준비 |
| 필요 시 실행 중단 | 학습 실행·상태 감시 |
|  | 성공/실패 상태 표시·Pod 종료 확인 |

### 데모 실행 흐름

```text
고정된 SD 1.5 LoRA 시나리오 확인
→ 실행 승인
→ Pod 생성
→ 학습 실행
→ 성공 또는 실패 표시
→ Pod 자동 종료 확인
```

정상 완료 화면에는 `Training completed` 로그, 종료 코드, 실행 시간, GPU 정보, Pod 자동 종료 완료를 표시한다.

## 차별점

MVP의 가치는 GPU를 더 쉽게 클릭하게 하는 데 있지 않다. 사용자가 직접 수행하던 아래 흐름을 실행 승인 하나로 대체하는 데 있다.

| 기존 흐름 | MVP 흐름 |
| --- | --- |
| GPU/VM 준비 | 고정 실행 계획 확인 |
| 환경 설정과 SSH 접속 | 실행 승인 |
| 학습 상태 확인 | 완료 상태 확인 |
| Pod 종료 |  |

데모는 실제 Pod 생성부터 학습 실행·종료 확인까지 보이며, 사용자가 인프라 콘솔을 열지 않았다는 점으로 UNWORK 주제 적합성을 증명한다.

## 타겟 및 시나리오

## 타겟 사용자

주 타겟은 Colab 또는 로컬 환경의 한계를 넘어 외부 GPU가 필요해졌지만, 클라우드 GPU 인프라 운영에는 익숙하지 않은 AI 개발자다. 초기 사용자 세그먼트는 AI 전공 학부생, 대학원 연구자, 주니어 AI 개발자, 개인 프로젝트 개발자다.

## 사용자 시나리오

사용자는 SD 1.5 LoRA 데모 시나리오 카드에서 학습 Repository와 실행 명령을 읽기 전용으로 확인한다. 실행 계획을 승인하면 시스템이 GPU Pod를 만들고 학습을 시작한다.

사용자는 웹 화면에서 2~3초 간격으로 바뀌는 상태를 확인한다. 학습이 끝나면 성공 로그, 종료 코드, 실행 시간, GPU 정보, Pod 자동 종료 결과를 받는다. 중단이 필요하면 `실행 중단`을 누를 수 있다.

## 성공·위험 요소

## 핵심 지표

MVP의 핵심 지표는 **사용자가 GPU 콘솔·SSH·환경 설정·Pod 종료를 직접 조작하지 않고, 실제 학습 실행과 Pod 종료 확인까지 도달한 비율**이다.

정상 데모의 사용자 개입은 실행 계획 확인과 실행 승인뿐이다.

## 리스크

| 리스크 | 최소 처리 정책 |
| --- | --- |
| Pod 생성 실패, 학습 명령 오류, timeout | `FAILED`와 원인 메시지 표시, Pod 종료 시도 |
| 사용자 중단 | 학습 중단 요청과 Pod 종료 시도 |
| Pod 종료 미확인 | 종료 상태를 폴링하고 확인 전까지 `종료 확인 중` 표시 |
| 다른 사용자가 실행 중 | 서비스 전체 동시 실행 1개만 허용하고, 대기열 없이 다시 시도하도록 안내 |
| 장시간 실행 | 학습 명령에 최대 10분 timeout 적용 |

## MVP 범위와 비목표

### 포함

- 익명 웹 세션별 Job 조회·중단 분리
- 고정된 SD 1.5 LoRA Golden Path와 사전 검증된 이미지
- 팀 Runpod 계정으로 단일 GPU Pod 실제 생성
- 실행 승인, 상태 폴링, 실행 중단
- 성공/실패 callback과 실제 Pod 종료 확인

### 제외

- 임의 Repository·실행 명령 분석과 다중 Golden Path
- 사용자 API 키 연결(BYOK), 로그인·팀 권한
- GPU 후보 비교, 다중 공급자, 동적 최적화
- 예산 입력, 실시간 비용 추적, 비용 가드레일
- OOM 재계획, 자동 재시도, 대기열
- artifact 전달·저장·다운로드
- 상세 로그 뷰어, 모델 결과 렌더링
