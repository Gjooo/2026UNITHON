# UNWORK 학습 실행 Agent — Product PRD

## 한 줄 정의

AI 개발자가 코드·실행 명령·예산·완료 조건을 제공하고 실행 계획을 승인하면, Agent가 가장 적합한 GPU 실행 환경을 결정하고 학습 실행부터 결과 전달·자원 정리까지 처리하는 학습 실행 Agent.

## 제품 목표

이 제품은 AI 학습 실행에 수반되는 GPU 인프라 운영 업무를 없애, 개발자가 문제 정의, 학습 목표 설정, 결과 판단에 집중하도록 돕는다.

사용자는 무엇을 학습할지, 얼마를 투자할지, 어떤 결과를 완료로 볼지를 결정한다. Agent는 실행 환경 비교, VM 생성, 환경 준비, 학습 실행·감시, 비용 통제, 결과 보관, 자원 종료를 수행한다.

## 배경

Colab이나 로컬 환경에서 학습하던 개발자는 VRAM 부족, 세션 종료, 실행시간 제한을 만나면 외부 GPU를 사용해야 한다. 외부 GPU를 쓰기 시작하면 공급자별 GPU 사양·가격·가용성을 비교하고, VM을 만들고, CUDA와 의존성을 맞추고, 학습 상태·비용을 확인하고, 결과를 회수한 뒤 VM을 종료해야 한다.

이 과정은 모델 개발과 다른 종류의 운영 업무다. 사용자는 GPU를 직접 관리하고 싶은 것이 아니라 학습 결과를 얻고 싶지만, 지금은 실행을 완료하기 위해 인프라 운영을 함께 수행해야 한다.

## 문제 및 해결 방안

## 사용자 문제

AI 개발자는 실행 가능한 학습 코드를 가지고 있어도 다음의 부담을 직접 감당한다.

- **실행 환경 판단**: 필요한 VRAM, GPU, 공급자, 예상 시간과 비용을 스스로 비교해야 한다.
- **환경 구축**: Repository 배포, Python·CUDA·의존성, 모델·데이터 접근을 준비해야 한다.
- **비용 통제**: 학습 작업 전체가 얼마가 들지 가늠하고, 예상보다 오래 걸리거나 실패할 때 비용을 관리해야 한다.
- **지속적 감시와 정리**: 정상 실행, 오류, checkpoint, 결과 보관, VM 종료를 계속 확인해야 한다.

## 해결 방안

사용자는 학습 Repository, 실행 명령, 예산, 완료 조건을 입력하고 사용할 GPU 공급자 계정을 연결한다. Agent는 코드와 실행 요구사항을 분석해 VRAM·프레임워크·의존성·예상 실행시간을 도출하고, 연결된 공급자들의 GPU 후보를 비교한다.

Agent는 `가장 저렴함`, `가장 빠름`, `균형형` 실행안을 제시한다. 각 안에는 GPU와 공급자, 예상 시간, GPU 사용료, Agent 실행 수수료, 예상 총비용, 위험도와 대체 후보가 포함된다. 사용자가 하나의 실행 계약을 승인하면 Agent는 해당 계약 안에서 환경을 만들고 학습을 실행한다.

실행 중 Agent는 상태·비용·최대 실행시간을 감시하고, 성공·실패·취소·예산 도달의 모든 경로에서 결과와 로그를 보존한 뒤 자원 종료를 확인한다. 사용자는 정상 경로에 개입하지 않고, 예산·시간·GPU가 바뀌는 재계획과 계속 투자 여부처럼 판단이 필요한 경우에만 승인한다.

### 사람과 Agent의 역할

| 사람의 판단 | Agent의 실행 |
| --- | --- |
| 학습 목표와 완료 조건 설정 | 실행 요구사항 분석 |
| 예산·최대 실행시간 설정 | GPU·공급자 후보 비교와 실행안 생성 |
| 실행 계약과 변경안 승인 | VM 생성·환경 준비·학습 실행 |
| 예외 상황에서 계속 투자 여부 결정 | 상태·비용 감시, 결과 보관, 자원 종료 확인 |

### 실행 계약

Agent가 실제 비용을 발생시키기 전 사용자는 다음을 확인하고 승인한다.

- 선택 GPU와 공급자, 선택 근거
- 예상 실행시간, GPU 사용료, Agent 실행 수수료, 예상 총비용
- 예산·최대 실행시간·자동 중단 조건
- 위험도, 대체 후보, 지원 환경 버전

실행 계약은 승인한 코드 버전, 실행 명령, 완료 조건, 예산, 시간 상한, 가격을 고정한다. GPU·비용·시간이 달라지는 변경은 새 계약으로 다시 승인받는다.

## 차별점

### 학습 작업 완료 비용으로 판단한다

시간당 GPU 단가가 아니라, 학습 작업을 완료하는 데 드는 예상 시간·GPU 사용료·저장소/전송비·실행 수수료를 합친 총비용으로 실행안을 비교한다. 중단 위험은 비용에 숨기지 않고 별도 위험도와 대체 후보로 설명한다.

### 인프라 조작을 실행 계약으로 대체한다

사용자는 GPU 종류, Region, CUDA 버전, VM 옵션, SSH 접속을 직접 입력하지 않는다. 학습 목적과 제약을 실행 계약으로 표현하면 Agent가 구체적인 인프라 조작을 수행한다.

### 실행의 전체 생애주기를 책임진다

Agent의 역할은 VM을 만드는 데서 끝나지 않는다. 실행 환경 준비, 학습 감시, checkpoint와 artifact 보관, 실패 로그 전달, 비용 통제, VM 종료 확인까지 하나의 Training Job으로 다룬다.

### 사람의 통제권을 남긴다

Agent는 정형화된 운영을 제거하지만, 예산과 완료 기준, 실행안 승인, 추가 비용을 감수할지에 대한 판단은 사용자에게 남긴다. 자동화가 판단을 대체하는 것이 아니라 인프라 운영을 대체한다.

## 타겟 및 시나리오

## 타겟 사용자

초기 사용자는 Colab이나 로컬 환경의 한계를 넘어 외부 GPU가 필요해졌지만, 클라우드 GPU 인프라 운영에는 익숙하지 않은 AI 개발자다. AI 전공 학부생, 대학원 연구자, 주니어 AI 개발자, 개인 프로젝트 개발자가 포함된다.

반복 학습을 수행하지만 별도 인프라 담당자가 없는 AI 스타트업·대학 연구실·사내 AI 프로젝트 팀은 장기 핵심 고객이다. 팀에는 프로젝트별 예산, 구성원별 한도, 실행 이력, 승인 정책을 제공한다.

## 사용자 시나리오

사용자는 Colab에서 LoRA 또는 다른 학습 작업을 시도하다 VRAM 부족이나 세션 종료를 겪는다. Repository와 실행 명령, 예산, 완료 조건을 입력하면 Agent가 여러 공급자의 실행안을 비교해 보여준다. 사용자는 예상 시간·비용·위험도를 확인하고 실행안을 승인한다.

이후 Agent는 학습 환경을 만들고 실행을 진행한다. 사용자는 작업 상태와 누적 비용을 확인하지만, 정상 경로에서는 GPU 콘솔·SSH·종료 버튼을 다루지 않는다. 완료 후에는 결과 artifact, 실행 시간, 실제/추정 비용, 종료 확인을 받는다.

## 성공·위험 요소

## 핵심 지표

핵심 지표는 **사용자가 서버를 직접 조작하지 않고, 학습 작업을 결과 전달과 자원 종료까지 완료한 비율**이다.

보조 지표는 실행 성공률, 예산 이내 완료율, 자동 종료 성공률, artifact 전달 성공률, 실행 계약 승인율, 사용자에게서 제거된 인프라 운영 단계 수다.

## 리스크

| 리스크 | 제품 정책 |
| --- | --- |
| GPU 가격·가용성 변동 | 승인 직전 가격·가용성을 재검증하고 변경 시 새 실행안을 제시 |
| 코드·환경 호환성 | 지원 Workload 계약과 검증된 실행 환경을 관리하고, 불확실한 요구사항은 실행 전 표시 |
| 비용 예측 오차 | 예상·실제 비용을 분리하고 예산·시간 상한과 중단 정책을 적용 |
| 학습 중단·실패 | checkpoint·로그를 보관하고, 정책에 따라 재개 또는 재계획안을 제안 |
| 자원 미종료 | 모든 종료 경로에서 자원 종료를 요청·확인하고, 확인 실패를 별도 상태로 관리 |
| Provider 자격 증명 | 최소 권한, 안전한 보관·폐기, 사용자 Workload와의 분리 원칙 적용 |
