# UNWORK 학습 실행 Agent — MVP 구현 계획서

## 1. 구현 목표

해커톤 데모에서 다음을 실제로 증명한다.

> 사용자는 GPU 사양·가격·예상 시간의 비교, GPU 공급자 콘솔, SSH, CUDA 설정, Pod 종료를 직접 조작하지 않고 학습 작업을 완료한다.

MVP는 제품 전체의 범용 코드 분석·다중 공급자 운영을 구현하지 않는다. 대신 사전 검증한 학습 작업 하나와 소수의 검증된 GPU 실행 프로필 안에서, 사용자의 예산과 우선순위에 따라 Agent가 실행 환경을 선택하고 실제 Runpod Pod 실행·종료까지 처리한다.

이 범위의 핵심은 **GPU 선택을 고정하지 않는 것**이다. 학습 코드가 고정돼 있어도, Agent가 후보의 예상 시간·비용을 비교하고 실행안을 결정해야 사용자가 없앤 업무를 데모에서 보여 줄 수 있다.

## 2. 데모 Golden Path

### 고정 범위

| 항목 | MVP 기준 |
| --- | --- |
| 학습 작업 | SD 1.5 LoRA fine-tuning 한 종류 |
| 코드·명령 | 사전 검증된 Repository와 고정 실행 명령 |
| 실행 환경 | 사전 검증된 Docker/Runpod 템플릿 이미지 |
| 공급자 | 팀 Runpod 계정 하나 |
| GPU 후보 | 데모 전에 검증한 2~3개 Runpod GPU 실행 프로필 |
| 최대 실행시간 | 10분 |
| 결과물 | artifact 전달 없이 성공 로그·종료 코드·실행 시간·선택 GPU·Pod 종료 확인 표시 |

### 사용자가 결정하는 값

사용자는 학습 코드나 GPU를 직접 설정하지 않는다. 아래 두 제약만 입력한다.

- `최대 예산`: 이 금액 안에서 완료할 수 있는 실행안을 찾는다.
- `우선순위`: `저비용`, `균형`, `빠른 완료` 중 하나를 선택한다.

### Agent가 결정하는 값

Agent는 고정 workload의 필요 VRAM과 각 GPU 실행 프로필의 예상 시간·예상 GPU 비용을 비교한다. 예산 안의 후보 중 우선순위에 맞는 하나를 추천하고, 비교 근거와 함께 읽기 전용 실행 계획으로 보여 준다.

예상 가격과 시간은 데모 전에 검증한 프로필의 스냅샷에 근거한 **추정값**이다. 실제 청구액·실시간 비용 중단을 보장하지 않으며, 이 한계는 화면에 명시한다.

사용자는 Agent의 추천 GPU를 수정하지 않고 `실행 승인`만 한다. 실행 전 승인 과정은 GPU를 고르는 작업이 아니라, 비용이 발생하는 실행 계약을 승인하는 과정이다.

## 3. 사용자 흐름

```text
익명 세션 생성
→ 최대 예산·우선순위 입력
→ Agent가 후보 비교 및 실행안 추천
→ 읽기 전용 실행 계약 확인
→ 실행 승인
→ Pod 생성 중
→ 학습 실행 중
→ 성공 또는 실패 표시
→ Pod 자동 종료 확인
```

- 실행 중 프런트엔드는 2~3초마다 Job 상태를 폴링한다.
- 사용자는 `실행 중단`을 누를 수 있다.
- 다른 Job이 실행 중이면 대기열 없이 `다른 데모 실행 중`을 표시한다.
- 세션당 실제 실행은 1회만 허용한다. 서비스 전체 동시 실행도 1개다.
- 예산 안의 후보가 없으면 Job을 만들지 않고 `NO_ELIGIBLE_PLAN`을 보여 준다. 사용자가 GPU를 수동으로 고르거나 예산 밖 실행을 강행할 수 없다.

## 4. 구현 구조

```text
Web frontend
  └─ REST API (익명 세션·제약 입력·추천 Job 생성·시작·조회·취소)
       ├─ 고정 Workload + GPU 프로필 비교기
       └─ Long-running backend worker
            ├─ 선택된 Runpod Pod 생성·상태 조회·삭제
            ├─ 10분 timeout 감시
            └─ Job 상태 저장

Runpod Pod
  └─ 고정 학습 스크립트 실행
       └─ 완료/실패 callback → Backend
```

### 책임 분리

| 구성요소 | 책임 |
| --- | --- |
| 프런트엔드 | 제약 입력, 후보 비교·추천 근거 표시, 승인·취소 요청, Job 상태 폴링 |
| 백엔드 추천기 | 고정 workload의 후보 필터·시간/비용 추정·추천 실행 계약 생성 |
| 백엔드 Worker | 선택 GPU의 Pod 생애주기, timeout, callback 처리, 종료 확인 |
| Runpod Pod | 고정 학습 스크립트 실행, 성공/실패와 종료 코드 callback |

브라우저를 닫아도 백엔드 Worker는 Pod 상태와 종료를 계속 관리한다.

## 5. 상태와 처리 정책

```text
DRAFT → PROVISIONING → RUNNING → TERMINATING → COMPLETED
                                  ↘ FAILED
                                  ↘ CANCELLED
```

- `DRAFT`는 Agent가 추천한 실행 계약을 보여 주고, 아직 비용이 발생하지 않은 상태다.
- 학습 성공 callback과 종료 코드 `0`을 받으면 `TERMINATING`으로 전환한다.
- 실패 callback, Pod 생성 실패, 학습 명령 오류, 10분 timeout은 모두 짧은 원인 메시지와 함께 종료 처리를 시작한다.
- 사용자 취소는 `CANCELLED` 예정 결과와 함께 종료 처리를 시작한다.
- 성공·실패·취소·timeout의 모든 경로에서 Pod 삭제를 요청한다.
- Runpod 상태 조회로 Pod 종료를 확인하기 전에는 최종 상태를 표시하지 않는다.
- 재시도, OOM 재계획, checkpoint 복구는 구현하지 않는다.

## 6. API·데이터 구현 기준

- API: [API-spec.md](API-spec.md)
- 데이터 모델: [ERD.md](ERD.md)

구현할 사용자 API는 아래 다섯 개다.

| API | 용도 |
| --- | --- |
| `POST /api/v1/session` | 익명 세션 생성 |
| `POST /api/v1/jobs` | 제약을 받아 Agent 추천이 포함된 Draft Job 생성 |
| `GET /api/v1/jobs/{jobId}` | 추천 실행 계약과 Job 상태 조회 |
| `POST /api/v1/jobs/{jobId}/start` | Agent 추천안 승인과 Pod 생성 시작 |
| `POST /api/v1/jobs/{jobId}/cancel` | 실행 중단과 Pod 종료 시작 |

Pod만 호출하는 내부 API:

| API | 용도 |
| --- | --- |
| `POST /api/v1/internal/jobs/{jobId}/completion` | 성공/실패, 종료 코드, 짧은 메시지 callback |

## 7. 비밀값과 실행 제한

- 팀 Runpod API 키는 서버 환경변수 또는 Secret Vault에서만 읽는다.
- 클라이언트와 DB에는 Runpod API 키를 저장하거나 반환하지 않는다.
- 로그인·비밀번호 없이 익명 세션으로 Job 소유권을 분리한다.
- 세션당 한 번만 실제 실행하고, 전체 서비스에서 활성 Job은 하나만 허용한다.
- GPU 프로필의 Provider resource ID, 이미지, 실행 명령은 서버 상수로만 관리한다. 클라이언트 입력으로 변경할 수 없다.

## 8. 명시적 제외 범위

- 임의 Repository와 실행 명령 입력·분석
- 사용자 GPU 공급자 API 키 연결(BYOK)
- Runpod 외 다중 Provider의 실시간 비교
- 데모 프로필 밖의 임의 GPU 선택·동적 provisioning 설정
- 실시간 가격 조회·정확한 실제 청구액·실시간 비용 중단
- artifact 저장·전달·다운로드
- 실시간 로그 뷰어, 모델 결과 이미지 렌더링
- OOM 재계획, 자동 재시도, checkpoint 복구, 실행 대기열
- 계정 로그인, 팀 기능, 결제·정산

## 9. 데모 완료 기준

다음이 실제로 동작하면 데모 구현을 완료한 것으로 본다.

1. 웹에서 최대 예산과 우선순위를 입력하면, Agent가 2개 이상 GPU 후보의 예상 시간·비용을 비교해 하나를 추천한다.
2. 추천 실행 계약에는 선택 GPU, 예상 시간, 예상 GPU 비용, 선택 근거와 추정값 한계가 읽기 전용으로 표시된다.
3. 사용자는 GPU 콘솔·GPU 사양·SSH·CUDA 설정을 조작하지 않고 추천안을 승인할 수 있다.
4. 팀 Runpod 계정에서 Agent가 선택한 GPU 프로필의 실제 Pod가 생성된다.
5. 사전 검증된 학습 스크립트가 Pod에서 실행되고 성공 또는 실패 callback을 보낸다.
6. 웹 화면이 폴링으로 `PROVISIONING`, `RUNNING`, 최종 상태를 표시한다.
7. 정상 실행 시 완료 로그, 종료 코드, 실행 시간, 선택 GPU, Pod 종료 확인이 표시된다.
8. 취소·실패·timeout에서도 Pod 삭제가 요청되고 종료 확인 뒤 최종 상태가 표시된다.
