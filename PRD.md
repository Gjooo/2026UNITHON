# UNWORK 학습 실행 Agent — MVP PRD

## 1. 한 줄 정의

이미 GPU 공급자 계정이 있는 소규모 AI 팀이 **코드·예산·완료 조건만 승인하면**, Agent가 Runpod에서 LoRA 학습 환경을 선택·생성·실행·감시·정리하여 학습 결과물을 전달하는 서비스.

## 2. 제품 목표

AI 개발자가 학습 한 건을 실행하기 위해 반복하던 GPU 인프라 운영 업무를 없앤다. 사용자는 학습의 목적과 투자 한도처럼 사람이 내려야 할 판단만 내리고, Agent는 실행 환경 선택부터 artifact 전달과 서버 종료까지 수행한다.

MVP에서 서비스가 제거하는 업무는 다음 하나의 흐름이다.

1. GPU 사양과 가격을 조사하고 후보를 비교한다.
2. VM을 생성하고 학습 환경을 설정한다.
3. 학습을 실행하고 로그·비용·상태를 감시한다.
4. checkpoint와 artifact를 보관한다.
5. 인스턴스를 종료해 불필요한 비용을 막는다.

정상 경로에서 사용자는 이 다섯 단계에 개입하지 않는다.

## 3. 해커톤 주제 적합성

이 제품은 GPU를 더 쉽게 대여하는 앱이 아니라, 학습 1회에 딸려오던 **인프라 운영 업무 자체를 제거**한다. 사람에게는 문제 정의와 판단을, Agent에게는 정형화된 실행과 감시를 맡긴다.

| 사람에게 남기는 판단 | Agent가 없애는 운영 |
| --- | --- |
| 학습 코드와 실행 명령 선택 | VM 후보 조사·선택 |
| 최대 예산과 완료 조건 설정 | Pod 생성·환경 설정 |
| 제안된 실행 계획 승인 | 학습 실행·상태·비용 감시 |
| 예산 초과/지연 시 계속할지 판단 | checkpoint 보관·artifact 전달·종료 |

발표에서는 기존 흐름의 ‘클라우드 콘솔 → SSH → 환경 설정 → 비용 탭 → 종료’를 사용자가 열지 않고, `목표 설정 → 실행 승인 → artifact 수령`만 수행한 장면으로 이를 증명한다.

## 4. 타깃 사용자

### 주 타깃

Runpod 계정과 결제수단을 이미 보유했지만, 별도 인프라 담당자 없이 반복 학습을 수행하는 3~10명 규모의 AI 스타트업·대학 연구실·사내 AI 프로젝트 팀의 개발자.

이들은 GPU를 처음 써 보는 사람이 아니라, 이미 쓸 수 있지만 매번 VM 조사·생성·감시·종료를 본업 밖의 일로 느끼는 사람이다.

### 비타깃 및 이유

- 외부 GPU를 처음 쓰는 Colab 개인 사용자: 가입·결제수단 등록·크레딧 충전이라는 더 큰 진입 장벽은 BYOK MVP가 없애지 못한다.
- 모든 PyTorch/GitHub 저장소: 임의 코드·의존성·데이터·보안까지 보장하면 1박 2일 MVP의 신뢰성이 무너진다.
- 멀티클라우드 사용자: 공급자별 가격·가용성·스토리지·종료 의미 차이로 실행 정책이 복잡해진다.

## 5. 문제

소규모 AI 팀은 학습 코드를 갖고 있어도, 매 실행마다 어떤 GPU가 예산과 VRAM 조건에 맞는지 조사하고, Pod를 만들고, 의존성을 설치하고, 학습 상태와 비용을 감시한 뒤 종료해야 한다. 이 과정은 모델 성능을 높이는 일이 아니지만, 실패하면 학습 지연·예산 초과·미종료 비용을 만든다.

Runpod의 API 키를 보유한 사용자라도 키는 Pod 생성·중지·종료 권한을 주므로, 이를 직접 다루는 부담과 실수 위험이 남는다. 우리 제품은 그 권한을 사용자의 승인된 실행 계약 안에서만 쓰도록 제한한다.

## 6. 해결 방식

사용자는 Runpod API 키를 이번 작업에만 연결하고, GitHub 저장소/실행 명령, 최대 예산, 완료 조건을 입력한다. Agent는 지원되는 학습 계약 안에서 VRAM 요구량과 후보 GPU를 평가하고, 예상 시간·GPU 사용료·Agent 실행 수수료·예상 총비용을 제안한다.

사용자가 승인하면 Agent는 Runpod Pod를 생성하고 학습을 실행한다. 학습 중에는 비용과 상태를 감시하고 checkpoint를 보관한다. 완료 확인 뒤 artifact를 전달하고, Pod를 종료한다.

### 총비용 표시 원칙

MVP의 예상 총비용은 다음과 같이 설명 가능한 값만 합산한다.

`예상 총비용 = 예상 GPU 사용료 + 저장소/전송비(해당 시) + Agent 실행 수수료`

- GPU 사용료는 사용자 Runpod 계정에 직접 청구된다.
- MVP의 Agent 실행 수수료는 `₩0 (데모)`로 별도 표시하며 실제 결제하지 않는다.
- 중단 위험은 추정 금액에 숨기지 않고 `낮음/보통/높음`과 대체 후보로 표시한다.
- 예상 시간은 검증된 동일/유사 템플릿의 측정값 기반 추정임을 표시한다.

## 7. MVP 범위

### 지원 범위

- 공급자: Runpod Pod API만 지원
- 코드: 검증된 Hugging Face LoRA 학습 템플릿 또는 사전에 호환성을 확인한 공개 저장소 1~2개
- 인터페이스: 지정된 `train.py`, `requirements.txt`, 실행 명령, artifact 출력 경로
- GPU 후보: 사전에 정의한 6~10개 GPU 유형
- 완료 조건: 지정한 epoch/step 완료 및 checkpoint·artifact 업로드 확인
- 실행 단위: GPU 1개, 작업당 하나의 Pod

### 제외 범위

- 임의의 GitHub 코드 자동 분석·수정·실행 보장
- AWS/GCP/Azure 등 멀티클라우드 비교
- 팀 권한 관리, 청구·정산, 실제 실행 수수료 결제
- 모델 품질(loss 등) 보장
- 사용자 기존 Pod·볼륨의 조회·수정·종료

## 8. 핵심 사용자 흐름

1. 사용자가 ‘새 학습 실행’을 열고 GitHub URL, 실행 명령, 최대 예산, epoch/step 완료 조건을 입력한다.
2. 사용자는 Runpod API 키를 작업 세션에 연결한다.
3. Agent가 지원 가능 여부와 GPU 후보별 VRAM, 예상 시간, GPU 비용, Agent 수수료, 총비용, 위험도를 제시한다.
4. 사용자가 하나의 실행 계획과 `GPU 1개·최대 시간·최대 예산·표준 템플릿` 범위를 승인한다.
5. Agent가 `unwork-job-<id>` Pod만 생성하고, 템플릿 환경에서 학습을 시작한다.
6. 사용자는 상태·누적 비용·예산 대비 비율을 확인하지만 정상 경로에서는 조작하지 않는다.
7. Agent가 완료 artifact, 총 소요 시간, GPU 실제 사용료 추정, 자동 종료 결과를 전달한다.

## 9. 예외 및 안전 정책

| 상황 | Agent 행동 | 사용자 행동 |
| --- | --- | --- |
| OOM | 더 큰 VRAM 후보로 1회 재계획 | 새 계획 승인 |
| Spot/인프라 중단 | 저장된 checkpoint에서 1회 재개 | 재개 실패 시 계속/중단 결정 |
| 예산 80% 도달 | 즉시 알림 | 필요 시 중단 결정 |
| 예산 100% 도달 | 자동 중단·artifact/checkpoint 보관 시도 | 계속하려면 새 실행 계약 승인 |
| 코드/의존성 오류 | 자동 중단, 오류 로그 전달 | 코드 수정 후 새 실행 |
| 앱 워커/브라우저 종료 | 독립 워커가 종료 재시도 | Pod ID와 직접 종료 경로 제공 |

### 키와 리소스 안전 원칙

- API 키는 작업 실행 중에만 사용하고, 완료·중단·오류 후 즉시 폐기한다. 계정에 재사용 목적으로 저장하지 않는다.
- 키 값은 로그·분석·오류 보고에 기록하지 않는다.
- Agent는 자신이 생성한 `unwork-job-<id>` 리소스 ID만 상태 확인·종료한다.
- 생성 뒤 최대 실행시간 종료 타이머를 등록하고, 백엔드 워커가 종료를 재시도한다.
- 자동 종료 실패 시 사용자에게 Pod ID와 직접 종료 방법을 즉시 제공한다.

## 10. 성공 기준

MVP의 핵심 성공 지표는 **사용자가 서버를 직접 조작하지 않고, 지원 범위의 학습 작업을 artifact 전달과 Pod 종료까지 완료한 비율**이다.

보조 지표는 다음과 같다.

- 실행 성공률
- 예산 이내 완료율
- 자동 종료 성공률
- artifact 전달 성공률
- 사용자에게서 제거된 인프라 운영 단계 수(목표: 5개)
- 제안된 총비용 및 실행 계획 승인율

## 11. 데모 완료 기준

1. 검증된 LoRA 저장소 하나를 입력해 Runpod Pod가 생성된다.
2. Agent가 두 개 이상 GPU 후보와 비용·시간·위험도 근거를 제시한다.
3. 승인 뒤 사용자의 SSH/클라우드 콘솔 조작 없이 학습 로그와 누적 비용이 표시된다.
4. checkpoint/artifact가 전달되고 Pod 종료 상태가 화면에 표시된다.
5. OOM 또는 예산 도달을 모의해 예외 정책과 사용자 승인 경계를 시연한다.

## 12. 이후 확장 가설

MVP의 실행 계약과 종료 신뢰성을 검증한 뒤, 팀별 예산·승인 정책·실행 이력, 추가 호환 템플릿, 다른 GPU 공급자 어댑터를 확장한다. 외부 GPU 초심자를 위한 완전 관리형 결제는 별도의 청구·환불·악용 방지 모델이 검증된 뒤 검토한다.
