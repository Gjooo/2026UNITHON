# UNWORK 학습 실행 Agent — PRD v2

## 한 줄 정의

GPU VM 공급자 계정을 사용하는 소규모 AI 팀이 **학습 목표·예산·완료 조건만 정하고 실행 계획을 승인하면**, Agent가 공급자별 LoRA 학습 환경을 비교·선택하고 실행·감시·결과 전달·VM 종료까지 수행하는 학습 실행 Agent.

## 제품 목표

이 제품은 GPU를 빌리는 일을 쉽게 만드는 것이 아니라, 학습 한 건에 딸려오던 GPU 인프라 운영 업무를 없앤다. 사용자는 학습의 목표, 투자할 예산, 완료 기준, 예외 상황에서 계속 투자할지처럼 사람이 내려야 할 판단만 한다. Agent는 GPU 후보 조사, VM 생성, 환경 준비, 실행·비용 감시, artifact 보관, VM 종료를 수행한다.

정상 경로에서 사용자는 GPU 공급자 콘솔·SSH·비용 탭을 열거나 종료 버튼을 누르지 않는다.

## 배경

GPU VM 공급자 계정과 결제수단을 보유한 소규모 AI 팀도 학습을 실행할 때마다 공급자별 GPU 사양·가격·가용성을 비교하고, VM을 생성하고, 의존성을 준비하고, 학습 상태와 비용을 감시하고, 결과물을 회수한 뒤 종료해야 한다. 이 일은 모델을 개선하는 일이 아니지만, 팀의 시간을 빼앗고 학습 지연·예산 초과·미종료 비용을 만든다.

이 MVP가 GPU 공급자 가입, 결제수단 등록, 최초 API 키 발급을 없애는 것은 아니다. 따라서 타깃은 외부 GPU를 처음 쓰는 개인이 아니라, 이미 하나 이상의 GPU VM 공급자를 사용할 수 있지만 반복 학습의 인프라 운영을 본업 밖의 일로 느끼는 팀이다.

## 문제 및 해결 방안

## 사용자 문제

별도 인프라 담당자가 없는 소규모 AI 팀은 학습 코드가 있어도 매 실행마다 GPU 선택, VM 생성, 환경 구축, 비용 감시, 결과물 회수, 종료를 직접 수행해야 한다. 어떤 GPU가 예산과 VRAM 조건에 맞는지 판단하기 어렵고, 작업을 끝낸 뒤 인스턴스가 남아 불필요한 비용이 발생할 수 있다.

## 해결 방안

사용자는 최초 권한 위임으로 자신이 사용할 GPU VM 공급자의 API 키를 작업에 연결하고, `학습 유형`, `모델/데이터 규모`, `최대 예산`, `완료 step`을 입력한다. Agent는 사전 측정한 벤치마크와 공급자별 가격·가용성 정보를 바탕으로 `가장 저렴함`, `가장 빠름`, `균형형` 세 실행안을 제시하고 예산 내 최저 예상 비용 안을 추천한다.

사용자가 실행 계약을 승인하면 Agent는 선택된 공급자에 VM을 생성하고 검증된 LoRA 학습 흐름을 실행·감시한다. 완료 후 artifact를 작업별 임시 저장소에 보관해 24시간 만료 링크로 전달하고, VM을 자동 종료한다.

### 사람과 Agent의 역할

| 사람의 판단 | Agent가 없애는 운영 |
| --- | --- |
| 학습 목표와 완료 조건 설정 | GPU 후보 조사·추천 |
| 최대 예산과 최대 실행시간 설정 | VM 생성·환경 준비 |
| 실행 계약 승인 | 학습 실행·로그·비용 감시 |
| 예외 시 계속 투자 여부 결정 | artifact 보관·VM 자동 종료 |

GPU 유형, 예상 비용 또는 예상 시간이 바뀌는 모든 재계획은 새 실행 계약으로 다시 승인받는다.

### 실행 계약

사용자는 승인 전 GPU 후보와 선택 이유, 예상 시간, GPU 예상 사용료, Agent 실행 수수료(`₩0 · 데모`), 예상 총비용, 위험도와 대체 후보, 최대 실행시간과 예산 90% 자동 중단 조건, 지원 학습 흐름/템플릿 버전을 확인한다.

`예상 총비용 = 예상 GPU 사용료 + 저장소/전송비(해당 시) + Agent 실행 수수료`

GPU 사용료는 사용자가 선택한 공급자 계정에 직접 청구된다. 비용 표시는 `경과 시간 × 승인된 시간당 GPU 가격`의 추정치이며 실제 청구액과 차이가 날 수 있음을 고지한다. 중단 위험은 총비용에 숨기지 않고 위험도와 대체 후보로 표시한다.

## 차별점

시간당 GPU 가격을 비교하거나 GPU를 대여하는 서비스가 아니다. 이미 GPU를 사용할 수 있는 팀이 학습 한 건을 실행하기 위해 수행하던 아래 여섯 단계를 실행 계약 하나로 대체한다.

| 기존 반복 학습 실행 | MVP 실행 |
| --- | --- |
| GPU 비교 | 학습 목표·예산·완료 조건 설정 |
| VM 생성 | 실행 계약 승인 |
| SSH/환경 설정 | artifact 수령 |
| 학습 실행 |  |
| 비용 감시 |  |
| artifact 회수·VM 종료 |  |

실행 화면은 `GPU 후보 선정 완료 → VM 생성 완료 → 환경 준비 완료 → 학습 감시 중 → artifact 보관 완료 → VM 자동 종료 완료` 타임라인을 보여주고, 각 자동 단계에 `사용자 조작 없음`을 표시한다. 이를 통해 UNWORK의 핵심인 ‘더 빠르게’가 아니라 ‘더 이상 존재하지 않게’를 증명한다.

## 타겟 및 시나리오

## 타겟 사용자

주 타겟은 하나 이상의 GPU VM 공급자 계정과 결제수단을 보유했고, LoRA 등 반복 학습을 수행하지만 별도 인프라 담당자가 없는 3~10명 규모의 AI 스타트업·대학 연구실·사내 AI 프로젝트 팀의 개발자이다.

제품은 다양한 GPU VM 공급자를 비교·선택하는 것을 목표로 한다. 다만 MVP의 실제 실행 어댑터는 Runpod 1개로 검증한다. 다른 공급자는 후속 어댑터로 확장하며, 데모에서 실제 연동하지 않은 공급자에 대해 실행 가능하다고 주장하지 않는다. 그 밖에 모든 GitHub/PyTorch 코드, 사용자 데이터 업로드, 팀 권한·정산은 제외한다. 지원 범위는 공개 데모 데이터셋, 사전에 정의한 6~10개 GPU 후보, GPU 1개·VM 1개, 검증된 Hugging Face LoRA 데모 흐름으로 한정한다.

## 사용자 시나리오

팀의 AI 개발자는 반복 LoRA 학습을 실행하려 한다. 사용자는 학습 목표 화면에서 학습 유형, 모델/데이터 규모, 최대 예산, 완료 step을 입력하고 사용할 GPU VM 공급자의 API 키를 작업에 연결한다. Agent가 공급자·GPU 유형이 포함된 세 가지 실행안을 제안하면 사용자는 하나의 실행 계약을 승인한다.

학습이 진행되는 동안 사용자는 실행 타임라인과 누적 추정 비용을 확인하지만 정상 경로에서는 추가 조작을 하지 않는다. 완료 후 사용자는 24시간 동안 유효한 artifact 다운로드 링크와 총 소요시간·추정 GPU 비용·VM 자동 종료 결과를 받는다.

## 성공·위험 요소

## 핵심 지표

MVP의 핵심 지표는 **지원 학습 흐름에서 사용자가 서버를 직접 조작하지 않고 artifact 전달과 VM 종료까지 완료한 비율**이다.

보조 지표는 실행 성공률, 예산 한도 내 종료율, VM 자동 종료 성공률, artifact 전달 성공률, 제거된 사용자 운영 단계 수(목표: 6개), 실행 계약 승인율이다.

## 리스크

| 리스크 | 정책 및 완화 |
| --- | --- |
| OOM | 더 큰 VRAM 후보로 1회 재계획 후 새 실행 계약 승인 |
| 인프라/Spot 중단 | checkpoint에서 1회 재개, 실패 시 계속/중단 판단 요청 |
| 예산 도달 | 80% 경고, 90% 자동 중단·checkpoint 보관 시도 |
| 코드/의존성 오류 | 자동 중단과 오류 로그 전달, 수정 뒤 새 실행 |
| API 키 유출 | 작업 중에만 사용하고 완료·중단·오류 후 즉시 폐기, 로그·분석에 미기록 |
| 자동 종료 실패 | 최대 실행시간 종료 타이머와 독립 워커 재시도, VM ID·직접 종료 방법 제공 |
| 사용자 기존 리소스 오조작 | Agent가 생성한 `unwork-job-<id>` VM만 조회·종료 |
| 데모 실패 | 정상 경로는 실제 Runpod에서 짧은 LoRA 학습까지 완주, OOM·예산·중단 예외는 동일 상태 머신의 결정론적 시뮬레이션으로 재현 |

## 최초 PRD 대비 핵심 변경점

| 항목 | 최초 PRD | v2 |
| --- | --- | --- |
| 주 타깃 | Colab 한계를 넘은 개인 개발자와 소규모 팀 | GPU VM 공급자 계정·결제수단을 이미 가진 소규모 팀 |
| 핵심 가치 | 작업 완료 총비용으로 GPU를 선택 | 반복 학습의 GPU 인프라 운영 6단계를 제거 |
| BYOK | 언급되지 않음 | 작업 단위 공급자 API 키 연결, 공급자에 직접 GPU 과금 |
| 코드 지원 | GitHub 학습 코드와 실행 명령 입력 | 검증된 LoRA 데모 흐름으로 한정, 모든 코드 실행은 제외 |
| 추천 근거 | 코드의 GPU/VRAM 요구량 분석 | 사전 측정 벤치마크와 모델/데이터 규모 프리셋 |
| 후보 제시 | GPU 후보 비교 | 저렴함·빠름·균형형 3안과 최저 비용 추천 |
| 총비용 | 구축시간·중단 위험까지 비용에 반영 | GPU·저장소/전송·수수료만 합산, 위험도는 별도 표시 |
| 비용 통제 | 예산 초과 시 예외 처리 | 80% 경고, 90% 자동 중단, 변경 시 재승인 |
| artifact | 보관한다고만 정의 | 24시간 만료 다운로드 링크와 자동 삭제 |
| 데모 | 구현 범위 미정 | 정상 경로 실연동, 예외는 상태 머신 기반 시뮬레이션 |
| UNWORK 증명 | 인프라 운영을 없앤다고 서술 | 기존 6단계 대 MVP 3단계 및 자동 실행 타임라인으로 증명 |
